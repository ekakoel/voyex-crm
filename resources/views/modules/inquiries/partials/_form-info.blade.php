<div class="module-card p-5 text-sm text-slate-600 dark:text-slate-300">
    <p class="text-xs font-semibold uppercase tracking-wide text-slate-500 dark:text-slate-400">{{ ui_phrase('Info') }}</p>
    <p class="mt-2 text-xs text-slate-500 dark:text-slate-400">
        {{ ui_phrase('Fill inquiry data clearly to help sales, reservation, and manager teams follow up faster.') }}
    </p>

    <div class="mt-4 space-y-3">
        <div>
            <p class="font-semibold text-slate-700 dark:text-slate-200">{{ ui_phrase('Customer') }}</p>
            <p class="mt-1 text-xs">{{ ui_phrase('Choose the correct customer record. This becomes the main reference for quotation, itinerary, and booking flow.') }}</p>
        </div>

        <div>
            <p class="font-semibold text-slate-700 dark:text-slate-200">{{ ui_phrase('Source') }}</p>
            <p class="mt-1 text-xs">{{ ui_phrase('Use source to mark where the lead comes from (for example referral, website, social media) so conversion tracking stays accurate.') }}</p>
        </div>

        <div>
            <p class="font-semibold text-slate-700 dark:text-slate-200">{{ ui_phrase('Deadline') }}</p>
            <p class="mt-1 text-xs">{{ ui_phrase('Set expected follow-up or response date to prevent overdue inquiries and keep handling priority visible.') }}</p>
        </div>

        <div>
            <p class="font-semibold text-slate-700 dark:text-slate-200">{{ ui_phrase('Priority') }}</p>
            <p class="mt-1 text-xs">{{ ui_phrase('Set urgency level. High priority should be handled first, while normal and low can follow regular queue.') }}</p>
        </div>

        <div>
            <p class="font-semibold text-slate-700 dark:text-slate-200">{{ ui_phrase('Notes') }}</p>
            <p class="mt-1 text-xs">{{ ui_phrase('Write key request details such as travel preference, special requirements, and important context for the next team.') }}</p>
        </div>
    </div>
</div>

